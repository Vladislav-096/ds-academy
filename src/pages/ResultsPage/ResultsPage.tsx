import { Results } from "../../components/Results/Results";

export const ResultsPage = () => {
  return (
    <section>
      <div className="container">
        <Results />
      </div>
    </section>
  );
};
