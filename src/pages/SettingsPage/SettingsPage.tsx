import { Settings } from "../../components/Settings/Settings";

export const SettingsPage = () => {
  return (
    <section>
      <div className="container">
        <Settings />
      </div>
    </section>
  );
};
